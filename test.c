#include <stdio.h>
#include <stdlib.h>
#include <math.h>


int main()
{
  int z, r // zeros, cases restantes
  r = n**2
  z = 70%r
  for(i=0; i<n; i++)
  {
    for(j=0; j<n; j++)
    {
      coucou = rand(1, r)
      if(coucou <= z)
      {
        tab[i][j] = 0;
        z--;
        r--;
      }
      else
      {
        r--
      }
    }
  }
  return 0;
}
